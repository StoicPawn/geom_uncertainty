# Ablations And Boundary Cases

Controls are organized under `experiments/controls/`:

- `random_subspaces`
- `euclidean_ablation`
- `shuffled_surprisal`
- `fisher_output_energy_control`

Boundary cases include OOD or detection settings where scalar entropy can remain stronger even though accessibility remains geometrically distinct.
